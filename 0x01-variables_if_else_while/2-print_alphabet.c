#include <stdio.h>

/**
 * main- Prints the alphabet using put char function in lower case
 * Return: value set to 0
 */
int main(void)
{
	char lower_case;

	for (lower_case = 'a'; lowercase <= 'z'; lower_case++)
	{
		putchar(lower_case);
	}
	putchar('\n');
	return (0);
}

#include <stdio.h>

/**
 * main- Prints the alphabet using put char function in lower case
 * Return: value set to 0
 */
int main(void)
{
	char lower_case;
	lower_case = 'a'

	while ( lowercase <= 'z')
	{
		putchar(lower_case++);
	}
	putchar('\n');
	return (0);
}
